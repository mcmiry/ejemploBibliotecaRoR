<p id="notice"><%= notice %></p>

<font color="Blue"><h1>Lista de Autores</h1></font>

<table cellspacing="5" cellpadding="3">
  <thead>
    <tr>
      <th>Id</th>
      <th>Nombre</th>
      <th>Apellidos</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @autors.each do |autor| %>
      <tr>
        <td><%= autor.id %></td>
        <td><%= autor.nombre %></td>
        <td><%= autor.apellidos %></td>
        <td><%= link_to 'Ver', autor %></td>
        <td><%= link_to 'Editar', edit_autor_path(autor) %></td>
        <td><%= link_to 'Eliminar', autor, method: :delete, data: { confirm: '¿Seguro que deseas eliminarlo?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<h2><%= link_to '+ Nuevo Autor', new_autor_path %></h2>
<br>

<h2><%= link_to 'Ir a Lista de Libros', libros_path%></h2>
